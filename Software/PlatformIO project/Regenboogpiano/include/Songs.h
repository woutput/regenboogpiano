#pragma once

#define MAXIMUM_NUMBER_OF_SONGS 20 // data types (may) need to change if this value changes
#define MAXIMUM_NUMBER_OF_NOTES_PER_SONG 50 // data types (may) need to change if this value changes
#define MAXIMUM_SONG_NAME_LENGTH 100

void setup_songs();
